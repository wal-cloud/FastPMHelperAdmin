<Window x:Class="FastPMHelperAddin.UI.UpdateActionDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Update Action"
        Height="300"
        Width="450"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Ball Holder -->
        <StackPanel Grid.Row="0" Margin="0,0,0,10">
            <TextBlock Text="New Ball Holder:" FontWeight="Bold"/>
            <TextBox x:Name="BallHolderTextBox" Margin="0,5,0,0"/>
        </StackPanel>

        <!-- Update Note -->
        <StackPanel Grid.Row="1" Margin="0,0,0,10">
            <TextBlock Text="Update Note:" FontWeight="Bold"/>
        </StackPanel>

        <TextBox Grid.Row="2"
                 x:Name="UpdateNoteTextBox"
                 TextWrapping="Wrap"
                 AcceptsReturn="True"
                 VerticalScrollBarVisibility="Auto"
                 Margin="0,0,0,10"/>

        <!-- Buttons -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Update"
                    Width="80"
                    Margin="0,0,10,0"
                    Click="UpdateButton_Click"
                    IsDefault="True"/>
            <Button Content="Cancel"
                    Width="80"
                    Click="CancelButton_Click"
                    IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
