<Window x:Class="FastPMHelperAddin.UI.CreateActionDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Create New Action"
        Height="450"
        Width="450"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Project -->
        <StackPanel Grid.Row="0" Margin="0,0,0,10">
            <TextBlock Text="Project:" FontWeight="Bold"/>
            <TextBox x:Name="ProjectTextBox" Margin="0,5,0,0"/>
        </StackPanel>

        <!-- Package -->
        <StackPanel Grid.Row="1" Margin="0,0,0,10">
            <TextBlock Text="Package:" FontWeight="Bold"/>
            <TextBox x:Name="PackageTextBox" Margin="0,5,0,0"/>
        </StackPanel>

        <!-- Title -->
        <StackPanel Grid.Row="2" Margin="0,0,0,10">
            <TextBlock Text="Action Title:" FontWeight="Bold"/>
            <TextBox x:Name="TitleTextBox" Margin="0,5,0,0"/>
        </StackPanel>

        <!-- Ball Holder -->
        <StackPanel Grid.Row="3" Margin="0,0,0,10">
            <TextBlock Text="Ball Holder:" FontWeight="Bold"/>
            <TextBox x:Name="BallHolderTextBox" Margin="0,5,0,0"/>
        </StackPanel>

        <!-- Description -->
        <StackPanel Grid.Row="4" Margin="0,0,0,10">
            <TextBlock Text="Description:" FontWeight="Bold"/>
        </StackPanel>

        <TextBox Grid.Row="5"
                 x:Name="DescriptionTextBox"
                 TextWrapping="Wrap"
                 AcceptsReturn="True"
                 VerticalScrollBarVisibility="Auto"
                 Margin="0,0,0,10"/>

        <!-- Buttons -->
        <StackPanel Grid.Row="6" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Save"
                    Width="80"
                    Margin="0,0,10,0"
                    Click="SaveButton_Click"
                    IsDefault="True"/>
            <Button Content="Cancel"
                    Width="80"
                    Click="CancelButton_Click"
                    IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
